// app/vendor-submission/layout.jsx
export const metadata = {
  title: 'Vendor Submission Form | Milam Industries LLC',
  robots: { index: false, follow: false }, // hide from search engines
};

export default function VendorSubmissionLayout({ children }) {
  return children;
}
